# Falcon Eye Frontend

React and Vite frontend for Falcon Eye.

Use the repository-level [README](../../README.md) for installation and
environment setup. Copy the frontend example before starting locally:

```bash
cp apps/frontend/.env.example apps/frontend/.env.local
pnpm frontend:start
```

`VITE_API_BASE_URL` must point to the backend without a trailing slash.
